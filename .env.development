# 开发环境（npm run dev 时自动加载）
NODE_ENV = development

# 当前环境标识
VITE_APP_ENV = development

# 接口基础路径：走 vite 代理，避免本地跨域
VITE_API_BASE_URL = /api

# 开发代理目标（真实后端地址，仅本地生效）
VITE_PROXY_TARGET = http://localhost:8080

# 是否开启 sourcemap
VITE_SOURCEMAP = true

# 是否移除 console / debugger
VITE_DROP_CONSOLE = false
